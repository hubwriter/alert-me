# Repository instructions

- After every change to Swift source, tests, project configuration, or app behavior, run the complete test suite with `./scripts/test.sh`.
- Do not report a code change as complete unless that command passes.
- Add or update tests whenever behavior changes.
- Keep the app native to macOS 26 and preserve Swift 6 strict concurrency.
